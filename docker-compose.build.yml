# Build and publish the jupyter image.
# Usage: docker compose -f docker-compose.build.yml build
#        docker compose -f docker-compose.build.yml push
services:
  jupyter:
    build:
      context: ..
      dockerfile: jupyter_docker/Dockerfile
      platforms:
        - linux/amd64
        - linux/arm64
    image: croeder/jupyter:latest
